import { OperadoraCelular } from './operadora-celular.model';

describe('OperadoraCelular', () => {
  it('should create an instance', () => {
    expect(new OperadoraCelular()).toBeTruthy();
  });
});
